- name: install needed system packages
  package: name={{ item }} state=installed
  with_items:
    - base-devel
    - btrfs-progs
    - python

- name: install cower
  include: aur-pkg.yml pkg_name=cower

- name: install pacaur
  include: aur-pkg.yml pkg_name=pacaur
